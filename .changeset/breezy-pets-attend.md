---
---
feat: adiciona script automatizado para criação de changesets e atualiza comando customizado do Cursor

- Cria script não-interativo `create-changeset.ts` para gerar changesets via linha de comando
- Adiciona pacote `scripts` com suporte para criação automatizada de changesets
- Atualiza comando customizado do Cursor para usar o script automaticamente
- Permite criar changesets sem interação manual, facilitando automação e CI/CD
